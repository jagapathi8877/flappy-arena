export interface UserData {
  id: string;
  rollNumber: string;
  name: string;
  bestScore: number;
}

export interface LeaderboardEntry {
  id: string;
  rollNumber: string;
  name: string;
  bestScore: number;
}
